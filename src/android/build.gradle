repositories {
    google()
    jcenter()
    flatDir {
        dirs '../app/libs'
    }
}

dependencies {
   api fileTree(dir: 'libs', include: ['*.jar, *.aar'])
   api(name:'android-sdk-6.16.1', ext:'aar')
}
